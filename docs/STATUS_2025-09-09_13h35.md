### Status do Projeto — 09/09/2025 - 13h35m

#### Visão Geral
- **Arquitetura**: Clean Architecture em uso (`core`, `infrastructure`, `interface`, `main`, `shared`).
- **Proxies**: Assertiva (3001) e Safeweb (3003) ativos, com autenticação e headers ajustados.
- **API Local**: Express em 3000 com rotas de auth, orders, webhooks e `protocols` (e-CPF/e-CNPJ).

#### e-CPF (A1)
- **Fluxo**: Biometria → Assertiva (DOB) → Consulta Prévia CPF (Safeweb) → Protocolo (Safeweb) → PIX (Safe2Pay).
- **Resultado**: ✅ Sucesso.
  - Protocolo gerado e PIX criado (referência = protocolo).
- **Ajustes aplicados**:
  - `DtNascimento` enviado como YYYY-MM-DD na Consulta Prévia (conforme doc).
  - `Authorization` no proxy: sem `Bearer` para Biometria e Consulta Prévia.
  - Rota `POST /protocols/ecpf` aceita `dataNascimento` opcional; normaliza formatos.

#### e-CNPJ (A1)
- **Fluxo**: Biometria (CPF resp.) → Assertiva (DOB) → Consulta Prévia CPF/CNPJ → Protocolo (Safeweb) → PIX.
- **Consulta Prévia**: ✅ OK (representante legal confirmado).
- **Protocolo**: ⛔ 500 na Safeweb.
  - Tentativas: Add/5 (e-CNPJ) e fallback Add/3; inclusão de chamada prévia a `EmitirCertificadoOnline` com `numeroSerie=0`.
  - Provável pendência do ambiente/produto: confirmar `idProduto`, `CnpjAR`, e/ou enviar `Modelo/Tipo/Validade` e `numeroSerie` real.
- **Rota auxiliar**: `POST /protocols/consulta_previa_cnpj` criada (normaliza `dataNascimento` e faz fallback Assertiva).

#### Correções recentes
- Corrigido erro de sintaxe em `src/core/usecases/CreateOrderUseCase.js` (servidor voltando a subir).
- Normalização de data na integração Safeweb (`protocolApis.js`).
- Logs e tratamento de erros aprimorados nas rotas `/protocols`.

#### Próximos Passos Recomendados
- Confirmar `idProduto` correto para e-CNPJ e `CnpjAR` do ambiente.
- Fornecer `numeroSerie` válido (ou parametrizar via `.env`) e incluir `Modelo` (1=A1), `Tipo` (2=eCNPJ), `Validade` (anos) no payload Add/5.
- Manter `Authorization` conforme doc (sem `Bearer` onde aplicável) e `DtNascimento` em YYYY-MM-DD.

#### Ambiente/Execução
- Proxies: `SAFEWEB_PROXY_PORT=3003`, `PORT=3001` (Assertiva).
- API: `node src/main/server.js` → http://localhost:3000.
- Rotas principais:
  - `POST /protocols/ecpf`
  - `POST /protocols/ecnpj/full`
  - `POST /protocols/consulta_previa_cnpj`


